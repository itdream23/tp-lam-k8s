apiVersion: v1
kind: Service
metadata:
  name: mysql
spec:
  selector:
    app: mysql
  ports:
    - port: 3306        # port exposé pour les autres pods
      targetPort: 3306  # port dans le conteneur MySQL
  type: ClusterIP       # service interne au cluster
